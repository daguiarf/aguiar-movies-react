services:
  web:
    container_name: aguiar_movies_web
    build: .
    ports:
      - "2109:2109"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=2109
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_TMDB_API_KEY=${NEXT_PUBLIC_TMDB_API_KEY}
